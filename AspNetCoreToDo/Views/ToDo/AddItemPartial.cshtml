@model ToDoItem

@{ 
    ViewData["Title"] = "Add a new item:";
}

<form asp-action="AddItem" method="POST">
    <label asp-for="Title">@ViewData["Title"]</label>
    <input asp-for="Title">
    <button type="submit">Add</button>
</form>